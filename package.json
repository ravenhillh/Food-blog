{
  "name": "foodblog-app",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client/blog && npm start --prefix server && npm run dev --prefix client/blog"
  }
}